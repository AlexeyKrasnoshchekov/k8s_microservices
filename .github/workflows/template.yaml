name: Docker Build and Push Template

on:
    workflow_call: # This makes the workflow reusable/callable
        inputs:
            serviceName:
                description: "Name of service to build"
                required: true
                type: string

jobs:
    build_adservice:
    # needs: test
    runs-on: ubuntu-latest
    steps:
        - name: Checkout code
          uses: actions/checkout@v4

        - name: Set up QEMU
          uses: docker/setup-qemu-action@v3

        - name: Set up Docker Buildx
          uses: docker/setup-buildx-action@v3

        - name: Log in to Container registry
          uses: docker/login-action@v3
          with:
              username: ${{ env.REGITRY_USER }}
              password: ${{ secrets.DC_TOKEN  }}

          # Extract metadata (tags, labels) for Docker
        - name: Docker Metadata action
          uses: docker/metadata-action@v5.0.0
          with:
              images: "${{ env.REGITRY_USER }}/${{ inputs.serviceName }}"

        - name: Build and push Docker image
          uses: docker/build-push-action@v6
          with:
              context: "${{ inputs.serviceName }}/"
              file: "${{ inputs.serviceName }}/Dockerfile"
              # platforms: linux/amd64,linux/arm64,linux/arm/v6,linux/arm/v7
              push: true
              tags: "${{ env.REGITRY_USER }}/${{ inputs.serviceName }}"
              labels: ${{ steps.meta.outputs.labels }}
